<ul id="products" class="inline product-listing" data-hook>
  <% products.each do |product| %>
    <%= render :partial => partial_name_for_product(product, :list_item), :locals => {:product => product} %>
  <% end %>
</ul>
