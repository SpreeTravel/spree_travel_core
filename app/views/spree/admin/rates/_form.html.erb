<div data-hook="admin_product_rates_form_fields" class="row">
  <div class="alpha four columns">
    <%= f.field_container :variant do %>
      <%= f.label :variant, Spree.t(:variant) %>
      <%= f.select :variant_id, @product.variants.map { |v| [v.sku, v.id] }, :class => 'select2 fullwidth' %>
    <% end %>
  </div>
</div>
