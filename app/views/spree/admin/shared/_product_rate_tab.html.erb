<%if @product.product_type%>
  <%= content_tag :li do %>
    <%= link_to_with_icon 'tasks',
                          Spree.t(:rates),
                          admin_product_rates_path(@product),
                          class: "nav-link #{'active' if current == :rates}" %>

  <% end if can?(:admin, Spree::Rate) && !@product.deleted? %>
<%end%>



