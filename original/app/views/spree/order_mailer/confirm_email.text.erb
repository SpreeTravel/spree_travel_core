Estimada/o cliente,

<%= t('email_introduction') %>

============================================================
Orden de venta <%= @order.number %>
============================================================
<% @order.line_items.each do |item| %>
<%= item.variant.product.name%>
<%= item.context.gsub("<br>", "\n") %>
<% end %>
============================================================
Subtotal: <%= number_to_currency @order.item_total, :precision => 0  %>
<% @order.adjustments.each do |adjustment| %>
<%= "#{adjustment.label}: #{number_to_currency adjustment.amount, :precision => 0 }"%>
<% end %>

Total: <%= number_to_currency @order.total, :precision => 0  %>

Su orden ha sido recibida por nuestro equipo de especialistas y estamos en su gestión. Tan pronto tengamos confirmación de su pedido nos pondremos en contacto con Ud.

Gracias por elegir a GrandSlam Limited

Mediante el uso de los servicios de GrandSlam y su sitio Ud. acepta los <%= link_to t('legal_terms'), 'http://www.grandslamtravelagency.com/legal_terms/' %>.
