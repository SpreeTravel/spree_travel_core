require 'spec_helper'

describe Spree::AppConfiguration, type: :model do
  # let (:prefs) { Rails.application.config.spree.preferences }
  #
  # it 'uses base travel searcher class by default' do
  #   prefs.searcher_travel_class = nil
  #   expect(prefs.searcher_travel_class).to eq Spree::Core::Search::SpreeTravelBase
  # end
  #
  # it { expect(Spree::Config).to have_preference(:use_cart) }

end
